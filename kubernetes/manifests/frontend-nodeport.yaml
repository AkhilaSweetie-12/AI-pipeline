apiVersion: v1
kind: Service
metadata:
  name: ai-platform-frontend-nodeport
  namespace: default
  labels:
    app: ai-platform-frontend
spec:
  selector:
    app: ai-platform-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    nodePort: 30080
  type: NodePort
