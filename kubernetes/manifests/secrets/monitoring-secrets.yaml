---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: grafana-admin-secret
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: grafana-admin-secret
    creationPolicy: Owner
  data:
  - secretKey: GF_SECURITY_ADMIN_PASSWORD
    remoteRef:
      key: dev-grafana-admin-password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: playwright-test-secret
  namespace: ai-platform
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: playwright-test-secret
    creationPolicy: Owner
  data:
  - secretKey: PLAYWRIGHT_TEST_PASSWORD
    remoteRef:
      key: dev-playwright-test-password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: playwright-admin-secret
  namespace: ai-platform
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: playwright-admin-secret
    creationPolicy: Owner
  data:
  - secretKey: PLAYWRIGHT_ADMIN_PASSWORD
    remoteRef:
      key: dev-playwright-admin-password
